from setuptools import setup

setup(
   name='stacksdk',
   version='1.0',
   description='A python sdk for Stack AI',
   author='baceituno',
   author_email='baceituno@getstack.ai',
   packages=[''],  #same as name
   install_requires=[], #external packages as dependencies
)